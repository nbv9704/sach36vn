# AI Working Notes

Before making changes, read `project_structure/info.md` first.

Use `project_structure/info.md` as the source of truth for:
- current project structure
- recent progress
- important decisions
- pending tasks

Avoid re-reading the whole project unless necessary.

After every meaningful code/config change, update `project_structure/info.md` with:
- what changed
- affected files
- current status
- next recommended steps